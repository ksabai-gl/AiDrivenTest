**Decision summary (for UI Decision artifact):** Profile `java-selenium-cucumber` confirmed by user (`cucumber`). Run via `mvn test -Dcucumber.filter.tags=@regression`. Forbidden: Vitest/Jest/`npm test`. Selenium locators: XPath only.

---
